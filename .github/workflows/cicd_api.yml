name: CI/CD for api

on:
    push:
        branches: [main]
        paths: 'api/**'

jobs:
    CI:
        runs-on: ubuntu-22.04

        strategy:
            matrix:
                node-version: [16.x]

        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-node@v3
              with:
                  node-version: ${{ matrix.node-version }}

            - run: yarn install
            - run: yarn a lint
            - run: yarn a pnpify prisma generate
            - run: yarn a test
            - run: yarn a build
