export enum PreferPositionTypes {
    webFrontend = 'webFrontend',
    appFrontend = 'appFrontend',
    backend = 'backend',
}

export enum PreferProjectTypes {
    all = 'all',
    business = 'business',
    learning = 'learning',
}

export enum PreferMeetingDays {
    all = 'all',
    workdays = 'workdays',
    weekends = 'weekends',
}
